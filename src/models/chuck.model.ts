export interface ChuckFact {
  id: string,
  fact: string,
  date: string,
  vote: string,
  points: string,
}

export interface ChuckFriend {
  username: string;
  latitude: string;
  longitude: string;
}
